import { MaxAndMinValueDirective } from './max-and-min-value.directive';

describe('MaxAndMinValueDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxAndMinValueDirective();
    expect(directive).toBeTruthy();
  });
});
